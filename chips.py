import grid

if __name__ == "__main__":

    from sys import argv

    # Check command line arguments
    if len(argv) is not 1:
        print("Usage: python3 chips.py [file]")
        exit(1)

    # Load the requested game or else Tiny
      
    #filename = f"data/{game_name}Adv.dat"
